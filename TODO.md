# TODO

## fchange

- [ ] `--list` flag — print all unreleased entries for a package
- [ ] `--edit` flag — open CHANGELOG.md in `$EDITOR` at the right line

## fcommit

- [ ] `--body` flag — multi-line body via `--body "..."` (just subject for now)

## General

- [ ] `--version` flag on both tools (read from `package.json`)
- [ ] Completions: zsh and fish support
- [ ] Completions: Windows / PowerShell support
- [ ] CI: smoke tests on both tools
